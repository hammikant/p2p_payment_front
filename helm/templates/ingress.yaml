apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "yc-clusterissuer"
spec:
  tls:
    - hosts:
        - des-web-app-python.despay.io
      secretName: domain-name-secret
  rules:
    - host: des-web-app-python.despay.io
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: des-web-app
                port:
                  number: 8080